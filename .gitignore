# 도구 상태 (커밋 대상 아님)
.omc/
.claude/settings.local.json

# M1부터
node_modules/
dist/
.vite/
*.tsbuildinfo
__pycache__/
*.pyc

# Piper 음성 모델 (61MB, 재다운로드 가능) — 커밋하지 않는다
tools/audio/voices/

# 생성 오디오(public/audio/us/*.mp3, 약 600KB)는 커밋한다.
# 파이썬 파이프라인 없이도 앱이 발음을 내도록. 재생성은 npm run audio.
